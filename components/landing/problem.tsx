export function Problem() {
  return (
    <section className="py-20 border-t border-navy-100 dark:border-navy-800">
      <div className="mx-auto max-w-4xl px-6 text-center">
        <h2 className="font-display text-2xl md:text-3xl font-semibold text-navy-900 dark:text-lavender-50">
          Most learning platforms sort students. Few actually teach them differently.
        </h2>
        <p className="mt-5 text-navy-600 dark:text-lavender-300 leading-relaxed">
          Traditional learning-management systems classify students into tracks and route them toward fixed content — the same video, the same PDF, the same quiz, regardless of what a specific student already understands or where they're stuck. Personalisation stops at the recommendation; the content itself never adapts. EduMind AI generates the explanation, the notes, and the practice material fresh for each learning gap it identifies.
        </p>
      </div>
    </section>
  );
}
